import { defineStyleConfig } from '@chakra-ui/react';

export const LazyImage = defineStyleConfig({
  baseStyle: {
    maxWidth: '100%',
    blockSize: 'auto',
    inlineSize: 'auto',
    rounded: 'md',
  },
});
